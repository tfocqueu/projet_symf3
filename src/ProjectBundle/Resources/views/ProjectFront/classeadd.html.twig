{% extends "::base.html.twig" %}

{% block body %}
    <h1>Ajout d'une classe</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Ajout d'une classe</button>
    {{ form_end(form)}}
{% endblock %}
