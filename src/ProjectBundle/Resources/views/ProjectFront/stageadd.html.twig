{% extends "::base.html.twig" %}

{% block body %}
    <h1>Ajout d'un stage</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Ajout du stage</button>
    {{ form_end(form)}}
    <script>
        $(document).ready(function() {
            $('.js-datepicker').datepicker({
                format: 'dd-mm-yyyy'
            });
        });
    </script>
{% endblock %}
